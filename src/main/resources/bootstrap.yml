spring:
  application:
    name: document-service
  cloud:
    config:
      uri: http://vtsign.tech:8888
server:
  port: 8300
  servlet:
    context-path: /document
tech:
  vtsign:
    zalopay:
      amount: 5000
