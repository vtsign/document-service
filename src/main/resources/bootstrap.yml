spring:
  application:
    name: document-service
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI:http://localhost:8888}
server:
  servlet:
    context-path: /document
tech:
  vtsign:
    zalopay:
      amount: 5000
