spring:
  application:
    name: document-service
  cloud:
    config:
      uri: http://vtsign.tech:8888
server:
  port: 8300
  servlet:
    context-path: /document
tech:
  vtsign:
    hostname: http://localhost:8765
    kafka:
      user-service:
        register: 'user-service-register'
      document-service:
        notify-sign: 'document-service-notify-sign'